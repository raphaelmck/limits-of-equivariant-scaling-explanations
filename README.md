# Limits of kernel and representation-based explanations for scaling in equivariant force fields

Analysis code, frozen result tables, figure scripts, and the experiment code for the paper. This
repository contains exactly what the submitted paper's figures, tables, and numerical claims
depend on — nothing exploratory, nothing superseded, no result that isn't reported.

**What is runnable from a clean clone of this repository:** every table, figure, and number the
paper reports, regenerated from the checked-in data in `data/` by a CPU-only pipeline. No GPU, no
dataset download, no network access, no checkpoint reads. This path is exercised by `make all`
and takes a few minutes on one core.

**What is not runnable from this repository:** regenerating `data/` itself — the force tangent
kernels, the intervention forward passes, and the matched-compute evaluations. That code is
provided in full under `pipeline/`, for audit rather than for execution: running it needs the
model checkpoints, the OMol25 splits, `torch`, `fairchem-core==2.14.0`, the upstream model
implementations, and a GPU. It has not been executed from a clean clone of this repository, and
`make` does not invoke it.

The model checkpoints and the OMol25 dataset are not part of this repository; the checkpoints each
result depends on are listed, with their identifying configuration and hash, in
`manifests/checkpoints.csv`.

## Quick start

```bash
python3 -m venv .venv && .venv/bin/pip install -e .   # numpy and matplotlib
make analysis    # regenerate every derived table into analysis_out/
make validate    # check those tables against the frozen ones in data/
make test        # check the submitted values against those tables
make figures     # render figures/figure1.pdf and figures/figure2.pdf

make check-manuscript TEX=path/to/main.tex   # paper numbers, read out of the LaTeX source
```

`make all` runs the first four. The whole pipeline takes a few minutes on one core.

## Paper results

| Paper result | Frozen input | Regenerated by |
|---|---|---|
| Sec. 2 scaling exponents, Fig. 1A frontier | `data/claim1/four_arch_force_checkpoint_table.csv` | `scripts/claim1/build_force_scaling.py`, `build_frontier_points.py` |
| Fig. 1B KRR learning-curve area | `data/claim1/dense_grid_krr_results.csv`, `owner_manifest.csv` | `scripts/claim1/build_dense_grid_ranking_comparison.py` |
| Fig. 1C, Table 1: GemNet-OC/eSEN crossover, 8 budgets | `data/claim1/dense_grid_krr_split_results.csv`, `dense_grid_owner_force_mse.csv` | `scripts/claim1/build_dense_grid_pairwise_gap.py` |
| Fig. 2A: block-9 ell=4 dose response, 4 checkpoints | `data/claim2/dose_response.jsonl`, `frontier_ell4_sensitivity_raw.jsonl` | `scripts/claim2/build_frontier_ell4_degree_balanced.py` |
| Fig. 2B, App.: matched-compute ell_max=2 vs. 4 | `data/claim2/ood_raw_per_config/` | `scripts/claim2/build_ood_curves_and_contrasts.py` |
| App.: depth localization (blocks 3, 6, 9) | `data/claim2/depth_localization/` | `scripts/claim2/build_depth_localization.py` |
| App.: independent training runs | `data/claim2/seed_replication_raw.jsonl` | `scripts/claim2/build_seed_replication_result.py` |
| App.: chemistry-family transfer | `data/claim2/ood_domain_labels.json` | `scripts/claim2/build_ood_domain_decomposition.py` |
| App.: matched frontier, 102 pairs | `data/claim2/lmax24_all_frontier_*.csv` | `scripts/claim2/build_lmax24_frontier.py` |

`make check-manuscript` verifies every one of these against the manuscript's own LaTeX source.
`paper/FIGURE_PROVENANCE.md` maps every panel of both figures to the file it reads.

**What this data does not support** (stated in full in the appendix; summarized here): the
high-order pathway is not shown to explain any scaling-exponent difference or any ell_max=4
advantage; dependence on high-order features is not shown to grow monotonically with compute;
the 102 matched-compute pairs are dense checkpoints from 11 training trajectories, not 102
independent observations; only the *sign* of the low-to-high causal-dependence effect is shown to
replicate across training runs, not its magnitude.

## Layout

```
data/            frozen inputs: per-configuration arrays, kernel and KRR results, matched pairs
pipeline/        the experiment code that produced data/ (audit copy; not run by make)
provenance/      per-file origin and checksums for pipeline/, with a checker
manifests/       every checkpoint each result depends on, with configuration and hash
src/             reusable analysis functions: bootstrap, interpolation, fitting, metrics
scripts/claim1/  kernel and scaling tables
scripts/claim2/  intervention and matched-compute tables
scripts/figures/ figure rendering, reading only from analysis_out/
scripts/validate.py  regenerated tables vs. frozen tables
tests/           paper numbers vs. regenerated tables
figures/         the two rendered paper figures
paper/           figure-provenance mapping
analysis_out/    generated; not checked in
```

## Provenance of the experiment code

Every file under `pipeline/` was copied from the source tree that produced `data/`.
`provenance/pipeline_manifest.json` records, per file, its original path and the SHA256 of both
the original and the released copy.

```bash
python3 provenance/check_pipeline.py                       # hashes and headers
python3 provenance/check_pipeline.py --source-root <path>  # also diff against the originals
```

With a source root, the checker compares abstract syntax trees with docstrings removed and the
release's renames undone, so it proves the released copies are *logically identical* to the
originals: comments and docstrings may differ, code may not. All 34 files pass.

`pipeline/model_patch/` holds the complete set of modifications made to the upstream model
implementations. They are additive instrumentation — they expose per-block hidden states and
change no forward computation, prediction, or force output.

## Reproducibility status

Every table and interval this repository reports reproduces exactly: point estimates involve no
randomness and match to 1e-9 relative, and every confidence interval has a documented upstream
bootstrap seed and call structure, so it reproduces bit-exact rather than merely overlapping.
`make validate` checks all of them.

`REPRODUCING.md` describes what would be needed to go further back than the frozen tables, to the
checkpoints themselves.
